사용하지 않는 코드나 에셋은 즉시 삭제하라.
금지: global 변수 사용, 하드코딩된 좌표 값
가이드라인:

#### 1. 개요
엔진/언어: Typescipt
핵심 목표: 동작 가능한 웹  메모 앱

####  2. 작업 규칙
모든 변경은: 설계 → 테스트 작성 → 구현 → 검증 순서로 진행
작업 완료 후 history.md에 한글로 이력 기록
readme.md는 한글로 유지
한 번에 하나의 이슈만 해결하고 테스트 통과 확인 후 git push 후 다음 아이템으로
관리 에이전트가 개발, 검증, 디자인 에이전트 관리
작업 완료 후 github 푸시

#### 3. 개발 주의 사항
실행시 외부 라이브러리 참조가 있으면 안됨
python -m http.server 8001 로 동작 가능해야 함

#### 4. 이미지, 사운드 리소스
이모지를 최대한 사용
인터넷 무료 이미지 리소스 사용시 출처 표기
인터넷 무료 오디오 리소스 사용시 출처 표기
오디오 리소스는 base64로 바꾸어 js에 포함 시킬 것

#### 5. 코딩 규칙 (Coding Rules)
모든 코드는 모듈화하여 작성 
변수명은 camelCase, 클래스명은 PascalCase 사용
주석은 코드의 의도를 명확히 설명할 것

#### 6. 시스템/메커니즘 (Game Mechanics)
DB: 브라우저 indexDB 사용
이어하기 지원

#### 6. 상세 지침 (Directives)
코드를 수정하기 전에 test 폴더의 기존 로직을 확인하라.
성능에 미치는 영향을 고려하여 최적화된 코드를 작성하라.
DB손상, 오류시에도 게임 동작해야함
폴더7 펼침화면, 열린 화면 모두 지원
폴더7 펼침 화면에서 고해상도로 보이도록 할 것
사용자의 진행 상항은 Browser의 IndexedDB의 디비에 저장해서 이어 하기 가능해야 함
다시 하기를 할 경우 사용자 컨펌 후 진행 해야 함
markdown파일 (.md)을 렌더링 해서 보여 주어야 함

#### 7. 인터페이스
PC 브라우저 동작 가능 해야함
모바일 터치 UI지원
키보드 마우스 지원

#### 8. 성능
5MB text 파일도 빠르게 처리 가능해야하며
200Kb이상의 파일을 읽으면, 로딩 애니메이션을 보여주어야 함

#### 9. 마무리
빌드시 index.html 하나의 산출물로 나오도록 하기
실행에 필요한 파일만 release 폴더로 복사하는 build.sh 를 만들 것
실행에 필요한 파일만 release 폴더로 복사하는 window cmd 용 build.bat 를 만들 것
Windows 용 build.bat 는 한글이 깨지지 않도록 할 것 (cp949 사용)
인터넷 검색 하여 좋은 아이디어가 있으면 추가 할 것